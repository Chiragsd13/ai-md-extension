node_modules/
dist/
*.vsix.bak
.DS_Store
Thumbs.db

# Keep the packaged VSIX for sideloading
!aimd-*.vsix

# Claude Code working files
.claude/

# OAuth tokens (stored in ~/.aimd/ — never in the repo)
*.aimd-tokens.json
